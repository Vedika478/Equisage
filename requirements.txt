# EquiSage — Core dependencies (pinned)
google-adk==2.3.0
google-genai>=2.10.0
yfinance==1.5.1
mcp==1.28.1
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0

# Test dependencies
pytest>=7.0.0
pytest-timeout>=2.1.0
pytest-asyncio>=0.23.0

# Optional — FastAPI server (for custom web UI backend)
fastapi>=0.100.0
uvicorn>=0.23.0
